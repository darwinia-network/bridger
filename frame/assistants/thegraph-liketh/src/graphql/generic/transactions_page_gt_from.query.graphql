query TransactionPage($from: Int!, $first: Int!) {
  transactionEntities(
    where: {
      blockNumber_gt: $from
    }
    first: $first
    orderBy: blockNumber
    orderDirection: asc
  ) {
    id
    origin
    blockNumber
    blockHash
    txHash
    txIndex
    txType
  }
}
