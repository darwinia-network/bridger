name: Check pull request

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    container:
      image: rust:1
    strategy:
      matrix:
        action: [clippy, test]
    steps:
      - uses: actions/checkout@v2

      - name: Specify toolchain
        run: |
          rustup default nightly
          rustup component add clippy
          rustup target add wasm32-unknown-unknown

      - name: Cache target
        uses: actions/cache@v2
        with:
          path: target
          key: ${{ runner.os }}-target-${{ hashFiles('Cargo.lock') }}
          restore-keys: ${{ runner.os }}-target-

      - name: Run cargo ${{ matrix.action }}
        run: cargo ${{ matrix.action }} --release --all --locked -- -D warnings

      - name: Optimize cache
        run: .maintain/optimize-cache.sh || exit 0
