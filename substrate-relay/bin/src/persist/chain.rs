// use chain_relay::types::transfer::ChainInfo;
//
// use crate::persist;
//
// impl persist::Chain {
// 	pub fn to_chain_info(&self) -> crate::error::Result<ChainInfo> {
// 		Ok(ChainInfo::new(
// 			self.entrypoint.clone(),
// 			self.signer.clone(),
// 			self.signer_password.clone(),
// 		)?)
// 	}
// }
