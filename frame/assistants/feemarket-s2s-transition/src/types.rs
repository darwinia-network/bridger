/// Strategy names
pub enum StrategyNames {
    /// Not have any changes
    Nothing,
    /// A reasonable strate, query fees from subscan and upadte it
    Reasonable,
    /// The first forever
    Crazy,
}
