query findJustification {
  justificationMappings (
    first: 1
    orderBy: BLOCK_NUMBER_DESC
  ) {
    nodes {
      id
      blockNumber
      blockHash
      mandatory
      justification
    }
  }
}
